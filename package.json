{
  "name": "the-bridge",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "mkdir -p dist && cp index.html app.js styles.css *.png dist/",
    "preview": "vite preview --host 0.0.0.0 --port 8080",
    "start": "serve -s dist -l tcp://0.0.0.0:${PORT:-8080}"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "serve": "^14.2.6"
  }
}
